# Client/Server runtime environment variables consumed by the app/server
# Copy to .env and set values in your environment. Do not commit secrets.
NG_APP_API_BASE=
NG_APP_NODE_ENV=development
NG_APP_FEATURE_FLAGS=allowFigmaOutbound
